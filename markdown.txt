# compile direct to pdf
pandoc document.md -o document.pdf

# read it in document-viewer
atril document.pdf
# this auto-reloads on re-compile

# latexdiff
pandoc -s document.md -o document.tex; latexdiff-git HEAD^ document.tex
# then use atril as before

# remember to latex and add to each commit
